{
  "name": "lovable-cloud-to-supabase-exporter-cloudflare-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker and Durable Object control plane for Lovable Cloud to Supabase export jobs.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "oxlint --deny-warnings .",
    "test": "vitest run",
    "check": "pnpm run typecheck && pnpm run test"
  },
  "dependencies": {
    "@dreamlit/lovable-cloud-to-supabase-exporter-core": "0.1.0"
  }
}
