name: Release

on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      dist_tag:
        description: "npm dist-tag"
        required: true
        default: "next"

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - run: pnpm test
      - run: pnpm build
      - name: Resolve dist tag
        id: dist
        shell: bash
        run: |
          TAG_INPUT="${{ github.event.inputs.dist_tag || '' }}"
          if [ -n "${TAG_INPUT}" ]; then
            TAG="${TAG_INPUT}"
          else
            TAG="latest"
          fi
          echo "tag=${TAG}" >> "${GITHUB_OUTPUT}"
      - name: Resolve CLI package version
        id: cli
        shell: bash
        run: |
          VERSION="$(node -p "require('./packages/cli/package.json').version")"
          echo "version=${VERSION}" >> "${GITHUB_OUTPUT}"
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push runtime image to GHCR
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./packages/container-runtime/Dockerfile
          push: true
          tags: |
            ghcr.io/dreamlit-ai/supabase-migrate-runtime:${{ steps.cli.outputs.version }}
            ghcr.io/dreamlit-ai/supabase-migrate-runtime:sha-${{ github.sha }}
      - name: Publish packages
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: "true"
        run: |
          DIST_TAG="${{ steps.dist.outputs.tag }}"
          pnpm release --tag "${DIST_TAG}"
